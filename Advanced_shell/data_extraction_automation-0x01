#!/bin/bash
jq -r '[.name, .types[0].type.name, .weight, .height] | @tsv' data.json | awk '{printf "%s %s %s %s\n", $1, $2, $3/10, $4/10}' | sed -E 's/^([^ ]+) ([^ ]+) ([^ ]+) ([^ ]+)$/\1 is of type \2, weighs \3kg, and is \4m tall./'
